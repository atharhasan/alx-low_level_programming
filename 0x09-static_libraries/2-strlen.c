#include "main.h"
#include <string.h>

/**
 * _strlen - function
 *
 * Description: function that returns the length of a string.
 *
 * @s: input
 *
 * Return: 0 always
*/

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
