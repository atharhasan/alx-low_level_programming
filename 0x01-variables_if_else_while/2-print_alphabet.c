#include <stdio.h>

/**
 * main - entry point
 *
 * Description: programe that print alphabet in lowercase
 *
 * Return:0 success
*/

int main(void)
{
	for (int i = 'A'; i <= 'Z'; i++)
	{
		char lower = tolower(x);

		putcher(lower);
	}
	return (0);
}
