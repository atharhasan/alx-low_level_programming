#include <stdio.h>

/**
 * main - entry point
 *
 * Description: programe that print alphabet in lowercase
 *
 * Return:0 success
*/

int main(void)
{
	char i = 'z';

	while (i >= 'a')
	{
		putchar(i);
		ch--;
	}
	putchar('\n');

	return (0);
}
